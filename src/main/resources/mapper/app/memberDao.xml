<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.app.dao.setting.MemberDao">

    <sql id="searchMemberCondition">
        <if test="queryMap.keyword != null and queryMap.keyword!=''">
            AND  (m.id = #{queryMap.keyword} or m.mobile = #{queryMap.keyword} or m.nick_name like concat('%',#{queryMap.keyword},'%'))
        </if>
        <if test="queryMap.minLat != null and queryMap.maxLat != null
                 and queryMap.minLng!=null and queryMap.maxLng != null">
            AND m.lat &gt;= #{queryMap.minLat} AND m.lat &lt;= #{queryMap.maxLat}
            AND m.lng &gt;= #{queryMap.minLng} AND m.lng &lt;= #{queryMap.maxLng}
        </if>
    </sql>

    <update id="updateLocationNumber">
        update member set lat = #{location.lat},lng=#{location.lng} where id=#{location.id}
    </update>


    <select id="searchMembers"  resultType="io.renren.modules.app.entity.setting.Member">
        select * from member m
        where  m.status = 1
        <include refid="searchMemberCondition"/>
        ORDER BY m.create_time DESC
        LIMIT #{page.offset},#{page.limit}
    </select>

    <select id="count" resultType="int">
        select count(*) from member m
        where  m.status = 1
        <include refid="searchMemberCondition"/>
    </select>

</mapper>